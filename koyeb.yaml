services:
  - name: gotenberg
    docker:
      dockerfile: Dockerfile
    ports:
      - port: 3000
        protocol: http
    health_checks:
      - http:
          path: /health
          port: 3000
